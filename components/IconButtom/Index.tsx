import * as React from 'react';
import {IconButton as Icb, IconButtonProps } from 'react-native-paper';

const IconButton = (props: IconButtonProps) => (
    <Icb {...props} />
);

export default IconButton;