import * as React from 'react';
import { ProgressBar as PB, ProgressBarProps } from 'react-native-paper';

const ProgressBar = (props: ProgressBarProps) => {
  return <PB {...props} />
};

export default ProgressBar;